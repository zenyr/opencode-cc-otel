{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://zenyr.github.io/opencode-cc-otel/schemas/telemetry.schema.json",
  "title": "OpenCode Telemetry Config",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional hosted schema URL for editor validation."
    },
    "channels": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "firstParty": {
          "$ref": "#/$defs/firstPartyChannel"
        },
        "secondParty": {
          "$ref": "#/$defs/secondPartyChannel"
        },
        "thirdParty": {
          "$ref": "#/$defs/thirdPartyChannel"
        }
      }
    },
    "providerFilter": {
      "$ref": "#/$defs/providerFilter"
    }
  },
  "$defs": {
    "httpsUrl": {
      "type": "string",
      "format": "uri",
      "pattern": "^https://",
      "description": "HTTPS telemetry endpoint."
    },
    "tokenValue": {
      "type": "string",
      "minLength": 1,
      "description": "Bearer token or env indirection like env:OPENCODE_CC_OTEL_HTTP_TOKEN."
    },
    "httpTarget": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "endpoint": {
          "$ref": "#/$defs/httpsUrl"
        },
        "token": {
          "$ref": "#/$defs/tokenValue"
        }
      },
      "required": ["endpoint"]
    },
    "httpConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "default": {
          "$ref": "#/$defs/httpTarget"
        }
      }
    },
    "otelConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "serviceName": {
          "type": "string",
          "minLength": 1,
          "description": "Override service.name for secondParty OTEL sink. Supports env:NAME."
        },
        "serviceVersion": {
          "type": "string",
          "minLength": 1,
          "description": "Override service.version for secondParty OTEL sink. Supports env:NAME."
        },
        "logsChannelId": {
          "type": "string",
          "minLength": 1,
          "description": "Override OTEL logs channel id. Supports env:NAME."
        },
        "metricsChannelId": {
          "type": "string",
          "minLength": 1,
          "description": "Override OTEL metrics channel id. Supports env:NAME."
        },
        "resourceAttributes": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra OTEL resource attrs for team or enterprise reporting."
        },
        "includeSessionId": {
          "type": "boolean",
          "description": "Include `session.id` in secondParty metric data point attrs when available."
        },
        "includeVersion": {
          "type": "boolean",
          "description": "Control `service.version` emission in secondParty metric resource attrs."
        },
        "includeAccountUuid": {
          "type": "boolean",
          "description": "Include `account.uuid` in secondParty metric resource attrs when `accountUuid` is set."
        },
        "accountUuid": {
          "type": "string",
          "minLength": 1,
          "description": "Explicit account UUID for secondParty metric resource attrs. Supports env:NAME."
        },
        "shutdownTimeoutMs": {
          "type": "integer",
          "minimum": 1,
          "description": "Best-effort flush timeout used for secondParty shutdown handling and HTTP write timeout."
        }
      }
    },
    "secondPartyHttpConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "endpoint": {
          "$ref": "#/$defs/httpsUrl"
        },
        "protocol": {
          "type": "string",
          "enum": ["http/json"],
          "description": "SecondParty HTTP transport protocol. Current implementation supports `http/json` only."
        },
        "headers": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Optional extra HTTP headers. Values may use `env:NAME` indirection."
        },
        "timeoutMs": {
          "type": "integer",
          "minimum": 1,
          "description": "Per-request timeout for secondParty HTTP transport."
        }
      },
      "required": ["endpoint"]
    },
    "secondPartyFileConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "NDJSON file path for secondParty file transport. Supports env:NAME."
        }
      }
    },
    "firstPartyChannel": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "FirstParty reporting defaults off. Set true to opt in."
        },
        "sink": {
          "const": "http",
          "description": "Anthropic 1P payload contract. Current delivery is HTTP batch only."
        },
        "http": {
          "$ref": "#/$defs/httpConfig"
        }
      },
      "required": ["sink"]
    },
    "secondPartyChannel": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "sink": {
          "type": "string",
          "enum": ["otel-json", "otlp-json"],
          "description": "SecondParty payload/export contract. `otel-json` writes Claude-style OTEL JSON envelopes. `otlp-json` exports official OTLP JSON payloads."
        },
        "transport": {
          "type": "string",
          "enum": ["file", "console", "http"],
          "description": "SecondParty delivery medium. `file` is the default local-safe path; `console` remains available for direct output; `http` is required for `otlp-json` export."
        },
        "file": {
          "$ref": "#/$defs/secondPartyFileConfig"
        },
        "http": {
          "$ref": "#/$defs/secondPartyHttpConfig"
        },
        "otel": {
          "$ref": "#/$defs/otelConfig"
        }
      },
      "required": ["sink"]
    },
    "thirdPartyChannel": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "const": false,
          "description": "thirdParty forwarding unsupported yet. Must stay false when present."
        }
      }
    },
    "providerFilter": {
      "type": "object",
      "additionalProperties": false,
      "description": "Filter telemetry by LLM provider. When set, only matching providers are logged.",
      "properties": {
        "allow": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true,
          "description": "Provider IDs to log (e.g. [\"anthropic\"]). Events from unlisted providers are dropped. Provider-agnostic events (tool, permission, command, diff) are only emitted while the active provider matches."
        }
      },
      "required": ["allow"]
    }
  },
  "examples": [
    {
      "providerFilter": {
        "allow": ["anthropic"]
      },
      "channels": {
        "firstParty": {
          "enabled": false,
          "sink": "http",
          "http": {
            "default": {
              "endpoint": "https://api.anthropic.com/api/event_logging/batch",
              "token": "env:OPENCODE_CC_OTEL_HTTP_TOKEN_1P"
            }
          }
        },
        "secondParty": {
          "enabled": true,
          "sink": "otlp-json",
          "transport": "http",
          "http": {
            "endpoint": "https://otel-collector.example.test",
            "protocol": "http/json"
          },
          "otel": {
            "serviceName": "claude-code",
            "serviceVersion": "env:OPENCODE_CC_OTEL_SERVICE_VERSION",
            "logsChannelId": "otel_3p_logs",
            "metricsChannelId": "otel_3p_metrics",
            "includeSessionId": true,
            "includeVersion": true,
            "resourceAttributes": {
              "user.subscription_type": "team"
            }
          }
        },
        "thirdParty": {
          "enabled": false
        }
      }
    }
  ]
}
